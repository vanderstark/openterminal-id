# sqlite directory

The following files will be populated here as part of a local environment. These files should not be tracked in `git`:


```
data
├── terminal.db
├── terminal.db-shm
└── terminal.db-wal
```
